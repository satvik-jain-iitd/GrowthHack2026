/* istanbul ignore file */
import { MetamodelLoading } from '../components/MetamodelLoading'

export default function Loading() {
    return (
        <MetamodelLoading
            title='Initiatives'
            subtitle='Browse and filter all initiatives, ETPs and ECMIs.'
        />
    )
}
